To use this Python program, please type the commands below in your terminal. Make sure Python's path is in your environment variables if you are on windows. Scores are tracked using MongoDB, please install MongoDB and associated materials such as MongoDB Compass before starting.


Links:
https://www.mongodb.com/try/download/community


Terminal Commands:
pip install pyttsx3
pip install keyboard
pip install pymongo
pip install playsound